<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_glass_blue">

    <!-- CONTENT -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- ================= TOOLBAR ================= -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/usersToolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:title="Manage Users"
            android:titleTextColor="#1E2A38"
            android:background="@drawable/bg_glass_toolbaar"
            android:padding="16dp"/>

        <!-- ================= FILTER + SEARCH CARD ================= -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_margin="12dp"
            android:padding="14dp"
            android:background="@drawable/glass_card_blue">

            <!-- Search -->
            <EditText
                android:id="@+id/etSearchUser"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:hint="Search name, email, phone"
                android:padding="12dp"
                android:background="@drawable/bg_search"
                android:inputType="text"
                android:drawableStart="@drawable/ic_search"
                android:drawablePadding="8dp"
                android:textColor="#1E2A38"
                android:textSize="14sp"
                app:hintTextColor="#7A8CA3"/>

            <!-- FILTER BUTTONS -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="10dp"
                android:gravity="center">

                <Button
                    android:id="@+id/btnWeek"
                    android:layout_width="0dp"
                    android:layout_height="42dp"
                    android:layout_weight="1"
                    android:text="Weekly"
                    android:textColor="#1E2A38"
                    android:background="@drawable/glass_filter_button"
                    android:layout_marginEnd="4dp"/>

                <Button
                    android:id="@+id/btnMonth"
                    android:layout_width="0dp"
                    android:layout_height="42dp"
                    android:layout_weight="1"
                    android:text="Monthly"
                    android:textColor="#1E2A38"
                    android:background="@drawable/glass_filter_button"
                    android:layout_marginStart="2dp"
                    android:layout_marginEnd="2dp"/>

                <Button
                    android:id="@+id/btnDate"
                    android:layout_width="0dp"
                    android:layout_height="42dp"
                    android:layout_weight="1"
                    android:text="Custom"
                    android:textColor="#1E2A38"
                    android:background="@drawable/glass_filter_button"
                    android:layout_marginStart="4dp"/>
            </LinearLayout>

            <!-- CLEAR FILTER -->
            <Button
                android:id="@+id/btnClearFilter"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:text="Clear All Filters"
                android:layout_marginTop="10dp"
                android:textColor="#FFFFFF"
                android:background="@drawable/glass_red_soft"/>

        </LinearLayout>

        <!-- ================= USERS LIST ================= -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_marginHorizontal="12dp"
            android:layout_marginBottom="12dp"
            android:background="@drawable/glass_card_blue"
            android:padding="8dp">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerUsers"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"/>
        </LinearLayout>

    </LinearLayout>

    <!-- ================= EXPORT FAB ================= -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabExport"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="20dp"
        android:src="@drawable/ic_export"
        android:tint="@android:color/white"
        app:backgroundTint="#3D6DF2"
        android:layout_gravity="bottom|end"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
