<?xml version="1.0" encoding="utf-8"?>
<!--
    item_category.xml
    Horizontal pill chip — compact icon + label side by side.
    Selected state: gold (#D4A853) border + gold icon tint.
    Unselected: dark card with muted icon.
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardCategory"
    android:layout_width="wrap_content"
    android:layout_height="48dp"
    android:layout_marginEnd="10dp"
    android:layout_marginTop="2dp"
    android:layout_marginBottom="2dp"
    android:clickable="true"
    android:focusable="true"
    app:cardCornerRadius="24dp"
    app:cardBackgroundColor="#0F1421"
    app:cardElevation="0dp"
    app:strokeWidth="1dp"
    app:strokeColor="#1E2A3E">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="14dp"
        android:paddingEnd="16dp">

        <!-- Icon badge -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardCategoryIconBg"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginEnd="9dp"
            app:cardCornerRadius="14dp"
            app:cardBackgroundColor="#1A2236"
            app:cardElevation="0dp">

            <ImageView
                android:id="@+id/imgCategoryIcon"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_gravity="center"
                android:scaleType="centerInside"
                android:tint="#8899B4"/>

        </com.google.android.material.card.MaterialCardView>

        <!-- Label -->
        <TextView
            android:id="@+id/tvCategoryTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="#8899B4"
            android:letterSpacing="0.01"/>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>