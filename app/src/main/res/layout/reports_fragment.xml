<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="#0B101A">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- 🔝 TOP BAR + TYPE FILTER -->
        <LinearLayout
            android:id="@+id/reportsTopBar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp"
            android:background="#0D1B2A"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Reports"
                android:textSize="22sp"
                android:textStyle="bold"
                android:textColor="#FFFFFF"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Detailed transaction history"
                android:textSize="13sp"
                android:textColor="#B0BEC5"
                android:layout_marginTop="2dp"/>

            <!-- ALL / INCOME / EXPENSE FILTER -->
            <com.google.android.material.chip.ChipGroup
                android:id="@+id/chipGroupFilter"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                app:singleSelection="true"
                app:selectionRequired="true"
                app:chipSpacing="10dp">

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipAll"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="All"
                    android:checkable="true"
                    android:checked="true"
                    style="@style/ChipExMate.All"/>

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipIncome"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Income"
                    android:checkable="true"
                    style="@style/ChipExMate.Income"/>

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipExpense"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Expense"
                    android:checkable="true"
                    style="@style/ChipExMate.Expense"/>
            </com.google.android.material.chip.ChipGroup>

        </LinearLayout>

        <!-- 🧠 INSIGHT CARD -->
        <androidx.cardview.widget.CardView
            android:id="@+id/cardInsight"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            app:cardCornerRadius="16dp"
            app:cardElevation="6dp"
            app:cardBackgroundColor="#0E1A2B"
            app:layout_constraintTop_toBottomOf="@id/reportsTopBar"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="14dp"
                android:id="@+id/txtInsight"
                android:text="⚠️ You spent ₹1,250 more on Food this month"
                android:textColor="#FFD54F"
                android:textSize="14sp"/>
        </androidx.cardview.widget.CardView>

        <!-- 🔍 FILTER CARD -->
        <androidx.cardview.widget.CardView
            android:id="@+id/cardFilters"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            app:cardCornerRadius="16dp"
            app:cardElevation="6dp"
            app:cardBackgroundColor="#0E1A2B"
            app:layout_constraintTop_toBottomOf="@id/cardInsight"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="14dp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Date Range"
                    android:textColor="#B0BEC5"
                    android:textSize="12sp"/>

                <TextView
                    android:id="@+id/txtDateRange"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Select date range"
                    android:textColor="#FFFFFF"
                    android:padding="10dp"
                    android:background="@drawable/bg_input_dark_rounded"
                    android:layout_marginTop="6dp"/>

                <Button
                    android:id="@+id/btnApplyFilter"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Apply Filters"
                    android:layout_marginTop="12dp"
                    android:backgroundTint="#1E88E5"
                    android:textColor="#FFFFFF"/>
            </LinearLayout>
        </androidx.cardview.widget.CardView>

        <!-- 📋 TRANSACTION LIST -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerReports"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:padding="12dp"
            android:clipToPadding="false"
            android:paddingBottom="100dp"
            app:layout_constraintTop_toBottomOf="@id/cardFilters"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- 🚫 EMPTY STATE -->
        <TextView
            android:id="@+id/tvEmpty"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="No transactions found"
            android:textColor="#90A4AE"
            android:textSize="14sp"
            android:visibility="gone"
            app:layout_constraintTop_toTopOf="@id/recyclerReports"
            app:layout_constraintBottom_toBottomOf="@id/recyclerReports"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- ⭐ EXPORT FAB -->
        <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
            android:id="@+id/fabExport"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Export"
            android:icon="@android:drawable/ic_menu_share"
            android:backgroundTint="#1E88E5"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="16dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>
