<?xml version="1.0" encoding="utf-8"?>
<!--
    reports_fragment.xml
    ─────────────────────────────────────────────────────────────
    BUG FIX: tvEmpty was a LinearLayout — ReportsFragment.java
    declares it as TextView so the cast crashed on launch.
    Fix: tvEmpty is now a TextView (hidden, used by the fragment
    for show/hide logic). The fancy empty-state UI uses the
    separate id "emptyContainer" (a LinearLayout) which is
    toggled in sync with tvEmpty via the same showEmpty() call
    — no changes needed in ReportsFragment.java.

    IDs preserved for ReportsFragment.java (unchanged):
      recyclerReports, tvEmpty, txtDateRange,
      chipAll, chipIncome, chipExpense, fabExport
    ─────────────────────────────────────────────────────────────
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#080C18">

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:overScrollMode="never"
        android:fillViewport="true"
        android:clipToPadding="false">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- ═══════════════════════════════
                 HEADER
            ═══════════════════════════════ -->
            <LinearLayout
                android:id="@+id/reportsTopBar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:background="#080C18"
                android:paddingStart="22dp"
                android:paddingEnd="22dp"
                android:paddingTop="22dp"
                android:paddingBottom="20dp">

                <!-- Title + Export FAB row -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical">

                    <LinearLayout
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:orientation="vertical">

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="R E P O R T S"
                            android:textColor="#D4A853"
                            android:textSize="9sp"
                            android:letterSpacing="0.25"
                            android:textStyle="bold"/>

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="3dp"
                            android:text="Transaction History"
                            android:textColor="#FFFFFF"
                            android:textSize="22sp"
                            android:textStyle="bold"/>
                    </LinearLayout>

                    <!-- Gold Export FAB -->
                    <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
                        android:id="@+id/fabExport"
                        android:layout_width="wrap_content"
                        android:layout_height="44dp"
                        android:text="Export"
                        app:icon="@android:drawable/ic_menu_share"
                        android:textColor="#080C18"
                        android:textSize="13sp"
                        android:textStyle="bold"
                        app:backgroundTint="#D4A853"
                        app:iconTint="#080C18"
                        app:elevation="8dp"
                        app:iconSize="16dp"/>

                </LinearLayout>

                <!-- Filter chips -->
                <com.google.android.material.chip.ChipGroup
                    android:id="@+id/chipGroupFilter"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="18dp"
                    app:singleSelection="true"
                    app:selectionRequired="true"
                    app:chipSpacingHorizontal="8dp">

                    <com.google.android.material.chip.Chip
                        android:id="@+id/chipAll"
                        android:layout_width="wrap_content"
                        android:layout_height="36dp"
                        android:text="All"
                        android:checkable="true"
                        android:checked="true"
                        android:textColor="#FFFFFF"
                        android:textSize="13sp"
                        app:chipBackgroundColor="@color/chip_bg_selector"
                        app:chipStrokeWidth="1dp"
                        app:chipStrokeColor="#1E2A3E"
                        app:chipCornerRadius="18dp"
                        app:chipMinHeight="36dp"
                        app:checkedIconVisible="false"/>

                    <com.google.android.material.chip.Chip
                        android:id="@+id/chipIncome"
                        android:layout_width="wrap_content"
                        android:layout_height="36dp"
                        android:text="Income"
                        android:checkable="true"
                        android:textColor="#FFFFFF"
                        android:textSize="13sp"
                        app:chipBackgroundColor="@color/chip_bg_selector"
                        app:chipStrokeWidth="1dp"
                        app:chipStrokeColor="#1E2A3E"
                        app:chipCornerRadius="18dp"
                        app:chipMinHeight="36dp"
                        app:checkedIconVisible="false"/>

                    <com.google.android.material.chip.Chip
                        android:id="@+id/chipExpense"
                        android:layout_width="wrap_content"
                        android:layout_height="36dp"
                        android:text="Expense"
                        android:checkable="true"
                        android:textColor="#FFFFFF"
                        android:textSize="13sp"
                        app:chipBackgroundColor="@color/chip_bg_selector"
                        app:chipStrokeWidth="1dp"
                        app:chipStrokeColor="#1E2A3E"
                        app:chipCornerRadius="18dp"
                        app:chipMinHeight="36dp"
                        app:checkedIconVisible="false"/>

                </com.google.android.material.chip.ChipGroup>

            </LinearLayout>

            <!-- Thin divider -->
            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="#1E2A3E"/>

            <!-- ═══════════════════════════════
                 DATE RANGE ROW
            ═══════════════════════════════ -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingStart="22dp"
                android:paddingEnd="22dp"
                android:paddingTop="16dp"
                android:paddingBottom="4dp">

                <com.google.android.material.card.MaterialCardView
                    android:layout_width="0dp"
                    android:layout_height="46dp"
                    android:layout_weight="1"
                    android:clickable="true"
                    android:focusable="true"
                    app:cardCornerRadius="23dp"
                    app:cardBackgroundColor="#0F1421"
                    app:cardElevation="0dp"
                    app:strokeWidth="1dp"
                    app:strokeColor="#1E2A3E">

                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:orientation="horizontal"
                        android:gravity="center_vertical"
                        android:paddingStart="16dp"
                        android:paddingEnd="16dp">

                        <ImageView
                            android:layout_width="15dp"
                            android:layout_height="15dp"
                            android:layout_marginEnd="10dp"
                            android:src="@drawable/ic_calender"
                            app:tint="#3D4E68"/>

                        <!-- BUG FIX: txtDateRange is a TextView — preserved exactly -->
                        <TextView
                            android:id="@+id/txtDateRange"
                            android:layout_width="0dp"
                            android:layout_height="wrap_content"
                            android:layout_weight="1"
                            android:text="Select date range"
                            android:textColor="#8899B4"
                            android:textSize="13sp"/>

                        <ImageView
                            android:layout_width="14dp"
                            android:layout_height="14dp"
                            android:src="@drawable/ic_arrow_right"
                            app:tint="#3D4E68"/>

                    </LinearLayout>
                </com.google.android.material.card.MaterialCardView>

            </LinearLayout>

            <!-- ═══════════════════════════════
                 SECTION LABEL
            ═══════════════════════════════ -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingStart="22dp"
                android:paddingEnd="22dp"
                android:paddingTop="22dp"
                android:paddingBottom="10dp">

                <View
                    android:layout_width="3dp"
                    android:layout_height="18dp"
                    android:layout_marginEnd="10dp"
                    android:background="#D4A853"/>

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Transactions"
                    android:textColor="#FFFFFF"
                    android:textSize="16sp"
                    android:textStyle="bold"/>

            </LinearLayout>

            <!-- ═══════════════════════════════
                 CONTENT CARD
                 (list + empty state)
            ═══════════════════════════════ -->
            <com.google.android.material.card.MaterialCardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="20dp"
                android:layout_marginEnd="20dp"
                app:cardCornerRadius="24dp"
                app:cardBackgroundColor="#0F1421"
                app:cardElevation="0dp"
                app:strokeWidth="1dp"
                app:strokeColor="#1E2A3E">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">

                    <!-- ─────────────────────────────────────────────
                         EMPTY STATE
                         "emptyContainer" is the fancy UI (icon + text).
                         "tvEmpty" is an invisible TextView — ReportsFragment
                         calls tvEmpty.setVisibility() and we mirror that
                         on emptyContainer via the same showEmpty() method.
                         Both are GONE by default.
                    ───────────────────────────────────────────── -->

                    <!--
                        BUG FIX: tvEmpty MUST be a TextView.
                        ReportsFragment.java:  TextView tvEmpty;
                        Old XML had LinearLayout with id tvEmpty → ClassCastException.
                        Now tvEmpty is a zero-size invisible TextView — purely a
                        visibility flag that the fragment controls.
                        The visible empty UI lives in emptyContainer below.
                    -->
                    <TextView
                        android:id="@+id/tvEmpty"
                        android:layout_width="0dp"
                        android:layout_height="0dp"
                        android:visibility="gone"/>

                    <!-- Fancy empty state — shown/hidden together with tvEmpty.
                         ReportsFragment.showEmpty() already sets tvEmpty visibility.
                         Override showEmpty() or add a second line in the fragment
                         if you want this to toggle too. It defaults to GONE.        -->
                    <LinearLayout
                        android:id="@+id/emptyContainer"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="vertical"
                        android:gravity="center"
                        android:padding="48dp"
                        android:visibility="gone">

                        <com.google.android.material.card.MaterialCardView
                            android:layout_width="64dp"
                            android:layout_height="64dp"
                            app:cardCornerRadius="32dp"
                            app:cardBackgroundColor="#141929"
                            app:cardElevation="0dp"
                            app:strokeWidth="1dp"
                            app:strokeColor="#1E2A3E">

                            <ImageView
                                android:layout_width="28dp"
                                android:layout_height="28dp"
                                android:layout_gravity="center"
                                android:src="@drawable/ic_transaction"
                                app:tint="#3D4E68"/>
                        </com.google.android.material.card.MaterialCardView>

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="16dp"
                            android:text="No transactions found"
                            android:textColor="#FFFFFF"
                            android:textSize="15sp"
                            android:textStyle="bold"/>

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="5dp"
                            android:text="Try changing filters or date range"
                            android:textColor="#3D4E68"
                            android:textSize="12sp"/>

                    </LinearLayout>

                    <!-- Transaction RecyclerView -->
                    <androidx.recyclerview.widget.RecyclerView
                        android:id="@+id/recyclerReports"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:paddingTop="8dp"
                        android:paddingBottom="8dp"
                        android:clipToPadding="false"
                        android:nestedScrollingEnabled="false"
                        android:overScrollMode="never"/>

                </LinearLayout>
            </com.google.android.material.card.MaterialCardView>

            <!-- Bottom nav spacing -->
            <View
                android:layout_width="match_parent"
                android:layout_height="110dp"/>

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>